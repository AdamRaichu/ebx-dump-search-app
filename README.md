# EBX Dump Search API

Minimal Node.js API for EBX dump search app.

> [!NOTE]
> The app expects to find a full xml dump at `full-xml` and a minified xml dump at `minified-xml`. Dumps are not expected to have been created with strings.txt.

Run locally:

```bash
npm install
npm start

```
